"""Carga demanda por cuenca: población INEI + derechos ANA + proxy NDVI agro.
"""
import logging
log = logging.getLogger("isht.etl.demanda")


def load_demanda_por_cuenca(csv_path: str):
    """Implementar en F1: CSV demanda → demanda consuntiva por cuenca."""
    raise NotImplementedError("Demanda pendiente — F1")
