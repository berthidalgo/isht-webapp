"""Balance hídrico por cuenca: oferta - demanda → brecha → semáforo.
Umbrales del plan: rojo < -0.2, azul > 0.1 (calibrar con §9).
"""
def calcular_balance(oferta, demanda):
    """Implementar en F1."""
    raise NotImplementedError("Balance pendiente — F1")
